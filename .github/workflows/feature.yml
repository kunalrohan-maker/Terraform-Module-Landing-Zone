name: Debug GitHub Runner

on:
  push:
    branches:
      - feature

jobs:
  debug:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Show Current Directory
        run: pwd

      - name: List Repository
        run: ls -R